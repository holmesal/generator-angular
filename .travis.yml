language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-realtime' ]; then cd .. && eval "mv $currentfolder generator-realtime" && cd generator-realtime; fi

